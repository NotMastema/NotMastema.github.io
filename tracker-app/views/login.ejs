<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Commission Tracker</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }

        .password-screen {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
        }

        .password-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border-radius: 24px;
            padding: 60px 50px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            max-width: 450px;
            width: 90%;
            text-align: center;
        }

        .password-container h1 {
            font-size: 32px;
            margin-bottom: 12px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 800;
        }

        .password-container p {
            color: #718096;
            margin-bottom: 32px;
            font-size: 14px;
        }

        .password-input {
            width: 100%;
            padding: 16px 20px;
            border: 2px solid rgba(102, 126, 234, 0.2);
            border-radius: 12px;
            font-size: 16px;
            margin-bottom: 20px;
            transition: all 0.3s ease;
            background: rgba(255, 255, 255, 0.8);
        }

        .password-input:focus {
            outline: none;
            border-color: rgba(102, 126, 234, 0.6);
            box-shadow: 0 0 0 4px rgba(102, 126, 234, 0.1);
        }

        .password-submit {
            width: 100%;
            padding: 16px 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
        }

        .password-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
        }

        .error-message {
            color: #f15d55;
            font-size: 14px;
            margin-top: 12px;
            display: <%= error ? 'block' : 'none' %>;
        }

        .lock-icon {
            font-size: 48px;
            margin-bottom: 20px;
        }

        .security-note {
            margin-top: 24px;
            padding-top: 24px;
            border-top: 1px solid rgba(0, 0, 0, 0.1);
            font-size: 12px;
            color: #a0aec0;
        }

        .security-note strong {
            color: #05c68e;
        }
    </style>
</head>
<body>
    <div class="password-screen">
        <div class="password-container">
            <div class="lock-icon">🔒</div>
            <h1>Commission Tracker</h1>
            <p>This application contains sensitive information and requires authentication.</p>
            <form method="POST" action="/login">
                <input
                    type="password"
                    name="password"
                    class="password-input"
                    placeholder="Enter password"
                    autocomplete="off"
                    autofocus
                    required
                />
                <button type="submit" class="password-submit">Unlock</button>
                <% if (error) { %>
                    <div class="error-message"><%= error %></div>
                <% } %>
            </form>
            <div class="security-note">
                <strong>Server-side authentication enabled</strong><br>
                Your session is protected with encrypted cookies and secure session management.
            </div>
        </div>
    </div>
</body>
</html>
